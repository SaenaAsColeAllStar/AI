id: platform-backend
name: Platform Backend Agent
type: platform
contract_version: "1.0"
description: Node/TypeScript REST APIs, database migrations, RBAC, and auth.
path: agents/backend/AGENT.md
entry: agents/backend/index.js
config: agents/backend/config.yaml
priority: high

dependencies:
  agents:
    - orchestrator
  shared: []

required_mcps:
  required:
    - filesystem-mcp
    - git-mcp
  recommended:
    - postgres-mcp
    - github-mcp

skills:
  required:
    - path: .agents/skills/teknovo-backend-development/SKILL.md
      id: teknovo-backend-development
  recommended:
    - path: .agents/skills/teknovo-chief-architect/SKILL.md
      id: teknovo-chief-architect

routing_keywords:
  - backend
  - api
  - rest
  - database
  - migration
  - rbac
  - auth
