{
  "$schema": "https://modelcontextprotocol.io/schema/config.json",
  "name": "filesystem",
  "description": "Filesystem MCP server — restricted directory access",
  "command": "npx",
  "args": [
    "-y",
    "@modelcontextprotocol/server-filesystem",
    "${WORKSTATION_REPO_ROOT}",
    "${TEKNOVO_REPO_ROOT}"
  ],
  "env": {
    "WORKSTATION_REPO_ROOT": "/workspace/AI",
    "TEKNOVO_REPO_ROOT": "/workspace/teknovo"
  },
  "notes": [
    "Replace path placeholders with actual clone locations.",
    "Add only directories the agent must read/write."
  ]
}
