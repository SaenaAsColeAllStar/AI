{
  "$schema": "https://modelcontextprotocol.io/schema/config.json",
  "name": "git",
  "description": "Git MCP server — local repository operations",
  "command": "npx",
  "args": ["-y", "@modelcontextprotocol/server-git"],
  "repositoryPaths": [
    "${WORKSTATION_REPO_ROOT}",
    "${TEKNOVO_REPO_ROOT}"
  ],
  "env": {
    "WORKSTATION_REPO_ROOT": "/workspace/AI",
    "TEKNOVO_REPO_ROOT": "/workspace/teknovo"
  },
  "notes": [
    "Remote operations (push) use system SSH config, not this template.",
    "Set repositoryPaths to your actual clone locations."
  ]
}
